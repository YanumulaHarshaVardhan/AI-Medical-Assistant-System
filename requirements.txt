streamlit
pandas
SpeechRecognition
googletrans==4.0.0rc1
gTTS

# Optional: audio input/output libraries
sounddevice
soundfile

# Avoid cgi: it's deprecated and removed in Python 3.13
# DO NOT include `cgi` — it's built-in in Python <3.13, removed in 3.13+

# PyAudio is tricky to install without system dependencies, prefer sounddevice/soundfile if possible
# If absolutely needed and portaudio is installed:
# pyaudio==0.2.14
